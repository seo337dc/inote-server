# 회원 관련 정책 — AUTH_POLICY.md

> `inote`/`inote-money`가 `inote-server`(공유 백엔드)를 함께 쓰면서 가입·로그인·프로필 관리가
> 정리 없이 만들어져 있던 상태에서, 2026-09-21 정책 정리 세션에서 확정한 내용.
> 실제 코드 점검 결과 가입/로그인은 동작하지만 **프로필 수정, 비밀번호 변경은 UI만 있고
> 실제 기능은 없는 목업 상태**였음 (`ProfileForm.tsx` — 저장 버튼이 `alert()`만 띄움).
> 구현 전에 전체 정책부터 정리한 문서.

---

## 1. 이메일 인증 — 보류

- 지금은 이메일 인증 없이 가입 즉시 로그인 (`emailAndPassword.requireEmailVerification: false`)
- 개인 프로젝트 규모에서 우선순위 낮다고 판단, 당장 도입 안 함
- **나중에 도입 시 2번(계정 연결) 정책도 같이 재검토 필요** — 지금은 이메일 인증이 없어서
  결과적으로 아래 2번 정책이 자연스럽게 유지됨

---

## 2. 이메일 가입 ↔ 구글 가입 — 완전 분리

- 같은 이메일이어도 **자동으로 연결/합쳐지지 않음**
- 예: `a@gmail.com`으로 이메일/비밀번호 가입한 사람이, 나중에 같은 `a@gmail.com` 구글 계정으로
  로그인 시도 → **별개로 처리, 자동 연결 안 함**
- **기술적 배경**: better-auth의 `account.accountLinking.requireLocalEmailVerified`(기본값
  `true`) 때문에, 이메일 인증이 없는 우리 시스템에선 이미 우연히 이렇게 동작 중이었음. 이걸
  `auth.ts`에 **명시적으로 설정**해서 "우연"이 아니라 "의도"로 고정할 것
- 사용자가 이 상황에서 로그인 실패하면, 지금은 안내 메시지가 부실함 → **에러 메시지 개선
  필요** ("이미 일반 회원가입으로 가입된 이메일입니다. 이메일/비밀번호로 로그인해주세요." 등)

---

## 3. `inote` ↔ `inote-money` 계정 공유 정책

**전제**: 두 서비스 모두 `inote-server`를 공유 백엔드로 쓰므로, 계정(`user` row) 자체는 이미
공유됨. 문제는 "계정은 같아도, 사용자가 실제로 양쪽 서비스를 다 쓰고 싶어 하는지는 다를 수
있다"는 것 — 어떤 사용자는 `inote`만, 어떤 사용자는 `inote-money`만, 어떤 사용자는 둘 다 쓸 수
있음.

**정책**:
- `user` 테이블에 서비스별 boolean 컬럼 추가: `usesInote`, `usesInoteMoney`
- 사용자가 한쪽 서비스(예: `inote`)에 가입되어 있는 상태로 **다른 서비스(`inote-money`)에
  처음 접속**하면:
  - 자동으로 로그인 처리하지 않고, **"이 계정으로 inote-money도 연동하시겠어요?" 확인 화면**을
    띄움
  - **"예"** → 해당 서비스의 boolean을 `true`로 설정, 정상 이용
  - **"아니오"** → **로그인 화면으로 돌려보냄** (세션 자체는 유지되므로 다른 서비스는 계속
    이용 가능 — 이 서비스만 "안 쓰는 것"으로 처리)

---

## 4. 프로필 필드 — 가입 방법 무관하게 동일 관리

- 대상 필드: `nickname`, `phone`, `image`(프로필 사진) — **전부 선택(optional) 입력**
- 구글 가입은 이름/이메일/프로필사진(구글이 자동 제공)만 채워지고 `nickname`/`phone`은
  비어있는 상태로 시작 → 이후 프로필 수정 화면에서 채울 수 있어야 함
- 이메일 가입도 동일하게 이 필드들을 나중에 채울 수 있어야 함
- **현재 미완성 상태인 프로필 수정 화면(`ProfileForm.tsx`)을 실제로 동작하게 구현하는 작업**이
  이 정책의 실행 항목

---

## 5. 비밀번호 관리 — 있는지 여부에 따라 다른 UI/API

| 상태 | 화면에 보여줄 것 | 쓸 API |
|---|---|---|
| 크레덴셜(이메일/비밀번호) 계정이 아예 없음 (구글로만 가입) | **"비밀번호 생성"** 버튼 | `setPassword` (현재 비밀번호 불필요) |
| 이미 비밀번호가 있음 | **"비밀번호 변경"** 폼 | `changePassword` (현재 비밀번호 필요) |

- 판단 기준: 해당 유저의 `account` 중 `providerId: 'credential'`이 있는지
- **기술 메모**: better-auth의 `setPassword`는 `serverOnly`라 클라이언트에서 직접 호출 불가 →
  `inote-server`에 래핑용 엔드포인트(예: `POST /users/set-password`) 신규 필요. 이미
  비밀번호가 있는 상태에서 `setPassword`를 호출하면 better-auth가 자체적으로
  `PASSWORD_ALREADY_SET` 에러로 막아줌(안전장치 내장)

---

## 6. 비밀번호 재설정(분실 시) — 보류

- 이메일 발송 서비스(Resend 등) 연동이 필요한 영역이라 일단 보류
- 다른 서비스들 사례를 직접 리서치한 뒤 다시 논의 예정

---

## 구현 시 필요한 변경사항 요약 (참고용)

**`inote-server`**
- `prisma/schema.prisma`: `user`에 `usesInote`, `usesInoteMoney` boolean 컬럼 추가
- `auth.ts`: `account.accountLinking` 명시적 설정 (2번 정책 고정)
- 에러 메시지 대응: 계정 연결 실패 에러 메시지 추가
- `users` 모듈: 프로필 수정 API(`nickname`/`phone`/`image` PATCH), `set-password` 래핑
  엔드포인트 추가

**`inote` (그리고 나중에 `inote-money`)**
- 프로필 수정 화면 실제 API 연동 (`ProfileForm.tsx` 목업 제거)
- 비밀번호 생성/변경 분기 UI
- 타 서비스 첫 진입 시 "연동 확인" 화면 (3번 정책)
